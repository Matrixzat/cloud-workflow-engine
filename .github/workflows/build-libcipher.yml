name: Build libcipher.so

# ─────────────────────────────────────────────────────────────────────────────
# AGENT INSTRUCTIONS — read this before pushing or triggering the build
#
# Source files live in the Replit workspace (Ultra dex2c mega/).
# Before triggering this workflow, make sure the cloud repo is up to date:
#
#   C sources  →  src/main/cpp/guard.cpp
#                 src/main/cpp/guard_pstrings.inc
#                 src/main/cpp/guard_mba.h
#                 Push to cloud repo:  cpp/guard.cpp
#                                      cpp/guard_pstrings.inc
#                                      cpp/guard_mba.h
#
#   Stub-loader Java sources  →  stub-loader/src/main/java/
#       com/ultra/dex2cvmp/ProxyApplication.java
#       com/secure/dex/utils/DexProtector.java
#       com/secure/dex/utils/Reflect.java
#       com/secure/dex/data/Const.java
#                 Push to cloud repo:  stub-loader/<same relative paths>
#
# After pushing, trigger this workflow with:
#   curl -X POST \
#     -H "Authorization: Bearer $GITHUB_PAT" \
#     -H "Accept: application/vnd.github+json" \
#     "https://api.github.com/repos/Matrixzat/cloud-workflow-engine/actions/workflows/build-libcipher.yml/dispatches" \
#     -d '{"ref":"main"}'
#
# When done, download artifact "cipher-binaries" and commit all files to
# src/main/assets/:  libcipher_arm64.a  libcipher_armeabi-v7a.a  stub.dex
# ─────────────────────────────────────────────────────────────────────────────
#
# Single workflow that does everything:
#
#  Job 1 — build-ollvm
#    Checks whether the OLLVM clang-19 release asset already exists.
#    If it does → skips all compilation steps (takes ~10 s).
#    If it doesn't → builds OLLVM clang-19 from TIMER-err/ollvm-19 with
#    AArch64-only target (~40–90 min), packages it, and uploads it as a
#    GitHub release asset tagged "ollvm-clang-linux-v2".
#    Re-run this job manually whenever you want to rebuild OLLVM from source.
#
#  Job 2 — build-guard
#    Downloads the OLLVM release asset, then compiles guard.cpp into
#    static archives for ALL four supported ABIs:
#
#      libcipher_arm64.a        (arm64-v8a  — OLLVM all 8 passes, real devices)
#      libcipher_armeabi-v7a.a  (armeabi-v7a — OLLVM all 8 passes, cross-compiled via --target=armv7a-linux-androideabi21)
#      libcipher_x86_64.a       (x86_64     — NDK clang, no OLLVM; emulator)
#      libcipher_x86.a          (x86        — NDK clang, no OLLVM; emulator)
#
#    All four archives must be committed to src/main/assets/ before rebuilding
#    the Dex2c Mega APK.
#
# After the workflow succeeds, download the artifact zip and copy all four .a
# files to src/main/assets/, then commit + push and rebuild the APK.
# ─────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:

# ─────────────────────────────────────────────────────────────────────────────
# JOB 1 — Build OLLVM clang (skipped if release asset already exists)
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  build-ollvm:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: write   # needed to create/update the release asset

    outputs:
      asset_id: ${{ steps.resolve.outputs.asset_id }}

    steps:
      - name: Check if OLLVM asset already exists
        id: check
        run: |
          ASSET_ID=$(curl -s \
            -H "Authorization: Bearer ${{ github.token }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/tags/ollvm-clang-linux-v2" \
            | jq -r '.assets[]? | select(.name=="ollvm-clang-linux-x86_64.tar.xz") | .id' \
            | head -1)
          echo "asset_id=${ASSET_ID}" >> $GITHUB_OUTPUT
          if [ -n "$ASSET_ID" ]; then
            echo "✅ OLLVM asset exists (ID: $ASSET_ID) — skipping rebuild"
          else
            echo "⚠️  No OLLVM asset found — will build from source"
          fi

      # ── All steps below are skipped when the asset already exists ───────────

      - name: Install build dependencies
        if: steps.check.outputs.asset_id == ''
        run: |
          sudo apt-get update -q
          sudo apt-get install -y --no-install-recommends \
            ninja-build cmake python3 git lld
          echo "Cores: $(nproc)"

      - name: Clone OLLVM source (shallow)
        if: steps.check.outputs.asset_id == ''
        run: |
          git clone --depth=1 https://github.com/TIMER-err/ollvm-19 /tmp/ollvm-src
          git -C /tmp/ollvm-src rev-parse HEAD > /tmp/ollvm-src-hash.txt
          cat /tmp/ollvm-src-hash.txt

      - name: Restore build cache
        id: cache-ollvm
        if: steps.check.outputs.asset_id == ''
        uses: actions/cache@v4
        with:
          path: /tmp/ollvm-install
          key: ollvm-install-${{ runner.os }}-AArch64-${{ hashFiles('/tmp/ollvm-src-hash.txt') }}

      - name: Configure CMake
        if: steps.check.outputs.asset_id == '' && steps.cache-ollvm.outputs.cache-hit != 'true'
        run: |
          cmake -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DLLVM_ENABLE_PROJECTS="clang" \
            -DLLVM_TARGETS_TO_BUILD="AArch64" \
            -DLLVM_INCLUDE_TESTS=OFF \
            -DLLVM_INCLUDE_EXAMPLES=OFF \
            -DLLVM_INCLUDE_DOCS=OFF \
            -DLLVM_INCLUDE_BENCHMARKS=OFF \
            -DLLVM_ENABLE_TERMINFO=OFF \
            -DLLVM_ENABLE_ZLIB=OFF \
            -DLLVM_ENABLE_ZSTD=OFF \
            -DCLANG_INCLUDE_TESTS=OFF \
            -DCLANG_INCLUDE_DOCS=OFF \
            -DCMAKE_EXE_LINKER_FLAGS="-fuse-ld=lld" \
            -DCMAKE_SHARED_LINKER_FLAGS="-fuse-ld=lld" \
            -DCMAKE_INSTALL_PREFIX=/tmp/ollvm-install \
            -B /tmp/ollvm-build \
            /tmp/ollvm-src/llvm

      - name: Build clang
        if: steps.check.outputs.asset_id == '' && steps.cache-ollvm.outputs.cache-hit != 'true'
        run: ninja -C /tmp/ollvm-build -j$(nproc) clang

      - name: Install clang
        if: steps.check.outputs.asset_id == '' && steps.cache-ollvm.outputs.cache-hit != 'true'
        run: |
          ninja -C /tmp/ollvm-build install-clang install-clang-resource-headers
          /tmp/ollvm-install/bin/clang --version

      - name: Smoke-test OLLVM passes (arm64)
        if: steps.check.outputs.asset_id == ''
        run: |
          echo "int main(){return 0;}" > /tmp/test.c
          /tmp/ollvm-install/bin/clang \
            --target=aarch64-linux-android26 \
            -mllvm -fla -mllvm -bcf -mllvm -sub \
            -fPIC -O2 -c /tmp/test.c -o /tmp/test_arm64.o
          echo "✅ OLLVM arm64 passes OK"

      - name: Package
        if: steps.check.outputs.asset_id == ''
        run: |
          tar -cJf /tmp/ollvm-clang-linux-x86_64.tar.xz \
            -C /tmp ollvm-install
          du -sh /tmp/ollvm-clang-linux-x86_64.tar.xz

      - name: Upload to release
        if: steps.check.outputs.asset_id == ''
        run: |
          # Create release (ignore error if already exists)
          REL_ID=$(curl -s -X POST \
            -H "Authorization: Bearer ${{ github.token }}" \
            -H "Content-Type: application/json" \
            "https://api.github.com/repos/${{ github.repository }}/releases" \
            -d '{"tag_name":"ollvm-clang-linux-v2","name":"OLLVM Clang (Linux x86_64)","body":"OLLVM clang-19 built from TIMER-err/ollvm-19 with AArch64-only target. All 8 obfuscation passes: fla bcf sub sobf split ibr icall igv.","prerelease":true}' \
            | jq -r '.id // empty')
          if [ -z "$REL_ID" ]; then
            REL_ID=$(curl -s \
              -H "Authorization: Bearer ${{ github.token }}" \
              "https://api.github.com/repos/${{ github.repository }}/releases/tags/ollvm-clang-linux-v2" \
              | jq -r '.id')
          fi
          echo "Release ID: $REL_ID"
          # Delete old asset if present
          OLD=$(curl -s \
            -H "Authorization: Bearer ${{ github.token }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/$REL_ID/assets" \
            | jq -r '.[] | select(.name=="ollvm-clang-linux-x86_64.tar.xz") | .id')
          [ -n "$OLD" ] && curl -s -X DELETE \
            -H "Authorization: Bearer ${{ github.token }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/assets/$OLD"
          # Upload new asset
          curl -X POST \
            -H "Authorization: Bearer ${{ github.token }}" \
            -H "Content-Type: application/x-xz" \
            "https://uploads.github.com/repos/${{ github.repository }}/releases/$REL_ID/assets?name=ollvm-clang-linux-x86_64.tar.xz" \
            --data-binary @/tmp/ollvm-clang-linux-x86_64.tar.xz \
            | jq '{id, name, size}'

      - name: Resolve asset ID (for build-guard job)
        id: resolve
        run: |
          ASSET_ID=$(curl -s \
            -H "Authorization: Bearer ${{ github.token }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/tags/ollvm-clang-linux-v2" \
            | jq -r '.assets[] | select(.name=="ollvm-clang-linux-x86_64.tar.xz") | .id' \
            | head -1)
          echo "asset_id=${ASSET_ID}" >> $GITHUB_OUTPUT
          echo "OLLVM asset ID: $ASSET_ID"

# ─────────────────────────────────────────────────────────────────────────────
# JOB 2 — Build libcipher.so (arm64-v8a only, all 8 OLLVM passes)
# ─────────────────────────────────────────────────────────────────────────────
  build-guard:
    runs-on: ubuntu-latest
    needs: build-ollvm
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Android SDK / NDK
        uses: android-actions/setup-android@v3

      - name: Install NDK r25c (sysroot + libc++)
        run: |
          yes | sdkmanager "ndk;25.2.9519653" 2>/dev/null || true
          echo "NDK_ROOT=$ANDROID_SDK_ROOT/ndk/25.2.9519653" >> $GITHUB_ENV

      - name: Download OLLVM clang-19
        run: |
          ASSET_ID="${{ needs.build-ollvm.outputs.asset_id }}"
          echo "Downloading OLLVM asset ID: $ASSET_ID"
          curl -L -s \
            -H "Authorization: Bearer ${{ github.token }}" \
            -H "Accept: application/octet-stream" \
            "https://api.github.com/repos/${{ github.repository }}/releases/assets/$ASSET_ID" \
            -o ollvm-clang-linux-x86_64.tar.xz
          echo "Downloaded: $(du -sh ollvm-clang-linux-x86_64.tar.xz | cut -f1)"
          tar -xJf ollvm-clang-linux-x86_64.tar.xz
          chmod +x ollvm-install/bin/clang++
          echo "✅ OLLVM: $(ollvm-install/bin/clang++ --version | head -1)"
          echo "OLLVM_BIN=$PWD/ollvm-install/bin/clang++" >> $GITHUB_ENV

      - name: Resolve toolchain paths
        run: |
          TC="$NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64"
          # ── NORMAL NDK BUILD — comment these two lines and uncomment OLLVM line below to re-enable obfuscation ──
          echo "CLANG_ARM64=$NDK_LINK_ARM64"                                    >> $GITHUB_ENV
          # OLLVM: echo "CLANG_ARM64=$OLLVM_BIN"                                >> $GITHUB_ENV
          # NDK clang++ wrappers — carry all runtime libs; used for link (all ABIs)
          # and for compile of non-arm64 ABIs (NDK clang, no OLLVM).
          echo "NDK_LINK_ARM64=$TC/bin/aarch64-linux-android26-clang++"       >> $GITHUB_ENV
          echo "NDK_LINK_ARM32=$TC/bin/armv7a-linux-androideabi21-clang++"    >> $GITHUB_ENV
          echo "NDK_LINK_X86_64=$TC/bin/x86_64-linux-android21-clang++"      >> $GITHUB_ENV
          echo "NDK_LINK_X86=$TC/bin/i686-linux-android21-clang++"           >> $GITHUB_ENV
          echo "SYSROOT=$TC/sysroot"                                          >> $GITHUB_ENV

      - name: Confirm OLLVM passes active
        run: |
          # ── NORMAL NDK BUILD — OLLVM passes disabled ──────────────────────────
          # Uncomment the block below and remove the echo line to re-enable all 8 passes.
          echo "OLLVM_FLAGS=" >> $GITHUB_ENV
          echo "ℹ️  OLLVM passes disabled — using plain NDK compile for debugging"
          # OLLVM re-enable:
          # echo "int x;" > /tmp/probe.c
          # if "$OLLVM_BIN" --target=aarch64-linux-android26 \
          #      --sysroot="$SYSROOT" -mllvm -fla \
          #      -x c -c /tmp/probe.c -o /tmp/probe.o 2>/dev/null; then
          #   echo "OLLVM_FLAGS=-mllvm -fla -mllvm -bcf -mllvm -bcf_prob=80 -mllvm -bcf_loop=3 -mllvm -sub -mllvm -sub_loop=3 -mllvm -sobf -mllvm -split -mllvm -split_num=5 -mllvm -ibr -mllvm -icall -mllvm -igv" >> $GITHUB_ENV
          #   echo "✅ All 8 OLLVM passes confirmed: fla bcf(80%×3) sub(×3) sobf split(5) ibr icall igv"
          # else
          #   echo "OLLVM_FLAGS=" >> $GITHUB_ENV
          #   echo "⚠️  OLLVM passes not responding — building without obfuscation"
          # fi

      - name: Build libcipher archives (arm64-v8a + armeabi-v7a)
        run: |
          set -e
          SRC="cpp"
          TC="$NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64"

          # Static C++ runtime paths (suppresses auto-link of libc++_shared.so)
          CPP_STATIC_ARM64=$(find "$TC" -name "libc++_static.a" | grep "aarch64" | head -1)
          CPP_ABI_ARM64=$(   find "$TC" -name "libc++abi.a"     | grep "aarch64" | head -1)
          CPP_STATIC_ARM32=$(find "$TC" -name "libc++_static.a" | grep "arm-linux" | head -1)
          CPP_ABI_ARM32=$(   find "$TC" -name "libc++abi.a"     | grep "arm-linux" | head -1)

          COMMON_BASE=(
            --sysroot="$SYSROOT"
            -fPIC -O2 -g0
            -fno-unwind-tables -fno-asynchronous-unwind-tables
            -fexceptions -frtti
            -fvisibility=hidden
            -ffunction-sections -fdata-sections
            -D__ANDROID__ -DANDROID
            -DD2C_HAS_JNILOAD
            -DFONTS_DEBUG_LOG
            -I"$SRC"
          )

          COMMON_OLLVM=("${COMMON_BASE[@]}")
          if [ -n "$OLLVM_FLAGS" ]; then
            read -ra OF <<< "$OLLVM_FLAGS"
            COMMON_OLLVM+=("${OF[@]}")
          fi

          GUARD="$SRC/guard.cpp"

          # ── arm64-v8a — OLLVM (all 8 passes) ─────────────────────────────────
          echo "Building arm64-v8a (OLLVM)…"
          "$CLANG_ARM64" --target=aarch64-linux-android26 \
            "${COMMON_OLLVM[@]}" -std=c++17 \
            -c "$GUARD" -o guard_arm64.o
          "$NDK_LINK_ARM64" \
            -shared -fPIC -Wl,--gc-sections -Wl,--build-id=none \
            -Wl,-soname,libcipher.so \
            -nostdlib++ "$CPP_STATIC_ARM64" "$CPP_ABI_ARM64" \
            -llog -lz -lm guard_arm64.o \
            -o libcipher_arm64.so
          echo "  ✅ libcipher_arm64.so — $(wc -c < libcipher_arm64.so) bytes"
          # Static archive — libc++ NOT baked in (NdkBuilder adds -lc++_static at link time)
          "$TC/bin/llvm-ar" rcs libcipher_arm64.a guard_arm64.o
          echo "  ✅ libcipher_arm64.a  — $(wc -c < libcipher_arm64.a) bytes"

          # ── armeabi-v7a — OLLVM clang (ARM target is compiled in alongside AArch64) ──
          echo "Building armeabi-v7a (OLLVM)…"
          "$CLANG_ARM64" --target=armv7a-linux-androideabi21 \
            "${COMMON_OLLVM[@]}" -std=c++17 \
            -c "$GUARD" -o guard_arm32.o
          "$NDK_LINK_ARM32" \
            -shared -fPIC -Wl,--gc-sections -Wl,--build-id=none \
            -Wl,-soname,libcipher.so \
            -nostdlib++ "$CPP_STATIC_ARM32" "$CPP_ABI_ARM32" \
            -llog -lz -lm guard_arm32.o \
            -o libcipher_armeabi-v7a.so
          echo "  ✅ libcipher_armeabi-v7a.so — $(wc -c < libcipher_armeabi-v7a.so) bytes"
          "$TC/bin/llvm-ar" rcs libcipher_armeabi-v7a.a guard_arm32.o
          echo "  ✅ libcipher_armeabi-v7a.a  — $(wc -c < libcipher_armeabi-v7a.a) bytes"

      - name: Strip + harden binaries
        run: |
          TC="$NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64"
          STRIP="$TC/bin/llvm-strip"
          OBJCOPY="$TC/bin/llvm-objcopy"
          REMOVE_SECS=(.symtab .strtab .comment .note.android.ident .eh_frame .eh_frame_hdr)
          for SO in libcipher_arm64.so libcipher_armeabi-v7a.so; do
            "$STRIP" --strip-all "$SO"
            for SEC in "${REMOVE_SECS[@]}"; do
              "$OBJCOPY" --remove-section="$SEC" "$SO" 2>/dev/null || true
            done
            echo "  $SO → $(wc -c < "$SO") bytes"
            readelf -S "$SO" 2>/dev/null | grep '^\s*\[' | awk '{print "   ", $2}' | grep -v '^\s*\[Nr\]'
          done

      - name: Build stub.dex
        run: |
          set -e
          SDK="$ANDROID_SDK_ROOT"

          # Find the highest build-tools version that has d8
          D8=$(find "$SDK/build-tools" -name "d8" | sort -V | tail -1)
          # Find android.jar for compileSdk 34
          ANDROID_JAR=$(find "$SDK/platforms" -name "android.jar" | grep "android-34" | head -1)
          if [ -z "$ANDROID_JAR" ]; then
            yes | sdkmanager "platforms;android-34" 2>/dev/null || true
            ANDROID_JAR=$(find "$SDK/platforms" -name "android.jar" | grep "android-34" | head -1)
          fi
          echo "d8:          $D8"
          echo "android.jar: $ANDROID_JAR"

          # Compile all four stub-loader Java sources
          mkdir -p stub_classes
          javac -source 8 -target 8 \
            -cp "$ANDROID_JAR" \
            -d stub_classes \
            stub-loader/com/secure/dex/data/Const.java \
            stub-loader/com/secure/dex/utils/DexProtector.java \
            stub-loader/com/secure/dex/utils/Reflect.java \
            stub-loader/com/ultra/dex2cvmp/ProxyApplication.java

          # Convert class files → stub.dex via d8 (min API 21)
          "$D8" \
            --output . \
            --min-api 21 \
            --lib "$ANDROID_JAR" \
            $(find stub_classes -name "*.class")

          # d8 outputs classes.dex — rename to stub.dex
          mv classes.dex stub.dex
          echo "✅ stub.dex — $(wc -c < stub.dex) bytes"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cipher-binaries
          retention-days: 90
          path: |
            libcipher_arm64.so
            libcipher_arm64.a
            libcipher_armeabi-v7a.so
            libcipher_armeabi-v7a.a
            stub.dex

